#!/bin/bash

set -euo pipefail

if [ "${SKIP_PRE_PUSH_VERIFY:-0}" = "1" ]; then
  echo "[pre-push] Skipping repo verification because SKIP_PRE_PUSH_VERIFY=1"
  exit 0
fi

ROOT="$(git rev-parse --show-toplevel)"
cd "$ROOT"

echo "[pre-push] Running pnpm verify"
pnpm verify
